{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the Lagado main + agent windows",
  "windows": ["main", "agent"],
  "permissions": [
    "core:default"
  ]
}
