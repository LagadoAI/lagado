/models/
/vault/
/vm-images/
/.laputa-secure/
target/
node_modules/
*.gguf
*.safetensors
dist/
.DS_Store
lagado-agent/vendored/

# Large / secret / regenerable data dirs — stay local
models
vault
vm-images

# 2026-06-16 POLICY REVERSAL (user decision): docs/, LAPUTA HOW TO/, all plans and
# the master-plan PDFs are now COMMITTED. The dev machine is the single point of
# failure (a prior crash already lost the master plan once); the repo is private
# FOREVER and only the finished binary ships, so disaster-recovery beats secrecy.
# Do NOT re-ignore docs/plans, and never make the repo public.
__pycache__/
