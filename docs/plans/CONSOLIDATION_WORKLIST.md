# Consolidation work-list (2026-07-06) — build down, top first

The architecture (reasoner / monitor bank / deterministic skeleton / membrane) mostly EXISTS
(inventory 2026-07-05). This is the gap list, ranked by leverage, integrate-before-invent.

1. **Membrane-fluency panel — BUILD NOW (host-only).** Standing instrument for every model/quant
   decision: (a) binding margins in the candidate's own embedding space (last-token pooling,
   fail-closed thresholds), (b) grammar-emission smoke, (c) battery golds. Frozen reference set;
   baseline = Q4_K_M measured on creation. Unblocks the staged Qwen3-4B decision with data.
2. **Damage-event sensor (membrane sensor v0).** X11 XDamage listener in-guest: the compositor
   pushes "what changed where" — replaces pixel-polling for recorder + monitor inputs; immune to
   the entire 2026-07-06 trap class. Validate against recorded v8 episodes. Needs VM time.
3. **Settle monitor, round 2 (parked by decision 2026-07-06).** 2-consecutive-tick patience
   read-out + 2-3 more recording rounds → re-gate. Data whenever the VM next runs anyway.
4. **Emission shrink (token-starvation path).** Measure current emission token counts in the
   battery; move op-emission toward selection-shaped output (fewer tokens carrying decisions).
   The reason→emit probe exists (Rust bin) — adapt or supersede with battery-side measurement.
5. **App-phase retirements (when app work resumes).** ColBERT mean-pool embedder (:8082) is
   FALSIFIED by R1b for binding — replace with reasoner-served last-token embeddings; one model
   serves chat+grammar+embeddings (verified 2026-06-23).
6. **Standing OSWorld queue (unchanged):** per-grounding ablation run; second-app port.
